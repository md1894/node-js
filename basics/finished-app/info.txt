note:: command is preceded by $ symbol

$ npm init
:this command will generate package.json file, and in that json object you have to insert 'start'
:script, before creating that package.json file it will ask for the entry point

$ npm start

add any anonymous name in package.json ,example : APP
$ npm run start-server

$ npm install nodemon --save-dev

this command will install nodemon locally to the project and there by update package.json file

if i delete node_modules folder then simply hit command

$ npm install

it will again install all the packages mentioned in package.json file

:::: --> package-lock.json

package dependencies with exact version
